/**
 * Display order only — question/answer text lives in `faq.items.<id>` in
 * messages/*.json.
 */
export const faqIds = [
  "compatible",
  "own-number",
  "calls-texts",
  "when-starts",
  "when-install",
  "delivery-speed",
  "lost-qr",
  "hotspot",
  "check-data",
  "data-runs-out",
  "refund",
  "multi-country",
  "networks",
  "privacy",
] as const;
